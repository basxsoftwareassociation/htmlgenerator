name: htmlgenerator-ci

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Install mypy
      run: pip install mypy
    - name: Check types
      run: mypy ${{ github.workspace }}
